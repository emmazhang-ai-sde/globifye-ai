# Frontend Sync (reference files from the frontend team)

**Last updated:** 2026-07-16

Static design references delivered by the frontend team. Nothing here runs as a server and there is no build step: **open any `.html` file directly in a browser** (double-click, or `open <file>.html` from a terminal).

- `GlobiFYE-Front-end-DialForge-main-0708/` — DialForge UI mockups (dashboard, active call, onboarding, power dialer, etc.), one standalone HTML page per screen
- `Stitch.html`, `liveTranscription.html` — earlier single-page references
- `frontend-delivery-log.md` — what was delivered when, and by which team

These are reference material for the AI team, not the frontend team's live codebase. Do not edit them; if a screen changes, the frontend team delivers a new copy and it gets logged in `frontend-delivery-log.md`.
